Vector Store# llama_index.core.vector_stores Package# Vector stores. Classes# VectorStoreQuery([query_embedding, ...]) Vector store query. VectorStoreQueryResult([nodes, ...]) Vector store query result. MetadataFilters Metadata filters for vector stores. MetadataFilter Comprehensive metadata filter for vector stores to support more operators. MetadataInfo Information about a metadata filter supported by a vector store. ExactMatchFilter alias of MetadataFilter FilterCondition(value[, names, module, ...]) Vector store filter conditions to combine different filters. FilterOperator(value[, names, module, ...]) Vector store filter operator. SimpleVectorStore([data, fs]) Simple Vector Store. VectorStoreInfo Information about a vector store (content and supported metadata filters).